interface:
  display_name: "Evaluate"
  short_description: "Post-implementation review — summarizes what was built, then quizzes you on design decisions and tradeoffs."
  default_prompt: "Use the /evaluate skill: Post-implementation review — summarizes what was built, then quizzes you on design decisions and tradeoffs."
