interface:
  display_name: "Fix PR Review"
  short_description: "Reconcile and apply fixes from my own /pr-review output plus any external PR reviews."
  default_prompt: "Use the /fix-pr-review skill: Reconcile and apply fixes from my own /pr-review output plus any external PR reviews."
