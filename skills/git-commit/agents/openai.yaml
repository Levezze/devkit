interface:
  display_name: "Git Commit"
  short_description: "Git add and commit with conventional commit format."
  default_prompt: "Use the /git-commit skill: Git add and commit with conventional commit format."
