interface:
  display_name: "Handoff"
  short_description: "Generate a handoff document from the requesting side's perspective to another team, repo, or agent."
  default_prompt: "Use the /handoff skill: Generate a handoff document from the requesting side's perspective to another team, repo, or agent."
