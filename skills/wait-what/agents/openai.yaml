interface:
  display_name: "Wait What"
  short_description: "Stop. That last message did not land: re-pitch it."
  default_prompt: "Use the /wait-what skill: Stop. That last message did not land: re-pitch it."
policy:
  allow_implicit_invocation: false
